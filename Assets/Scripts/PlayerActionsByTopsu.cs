using UnityEngine;
using UnityEngine.InputSystem;

namespace AC9649
{
    
    public class PlayerActionsByTopsu : MonoBehaviour
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {

        }

        public void CreateSonarPing(InputAction.CallbackContext ctx)
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
